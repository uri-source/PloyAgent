# Copy to .env on your VPS:  cp .env.production.example .env
# Never commit .env with real secrets.

# --- Required: strong DB password (must match POSTGRES_PASSWORD in compose) ---
POSTGRES_PASSWORD=change-me-use-openssl-rand-hex-32
DATABASE_URL=postgresql://postgres:change-me-use-openssl-rand-hex-32@timescaledb:5432/ploy_agent

# Dashboard: localhost only; Cloudflare Tunnel forwards to 127.0.0.1:8765
WEB_HOST=127.0.0.1
WEB_PORT=8765

# Polymarket / agent (match your local focus)
POLY_GAMMA_TAGS=nba
POLY_GAMMA_EVENT_SLUGS=spacex-or-openai-higher-ipo-closing-market-cap,what-will-spacexs-ipo-valuation-be,what-will-openais-ipo-valuation-be
ENRICHMENT_ESPN_LEAGUES=nba
AGENT_STRATEGIES=cross_market_arb,baseline_model,player_adjust,behavior_fade
RANK_TOP_N=10
RANK_MERGE_BY_MARKET=true
MIN_EDGE_CENTS=3.0

# Reasoning (optional but recommended)
ANTHROPIC_API_KEY=

# Slack (optional)
SLACK_BOT_TOKEN=
SLACK_CHANNEL=
SLACK_EVENTS_PORT=8766

# Simulation forward loop (optional; or run replay manually)
SIM_FORWARD_INTERVAL_SEC=5

# TLS: leave false on Linux VPS unless you hit cert errors
PLOY_INSECURE_SSL=false
